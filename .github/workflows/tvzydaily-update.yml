name: TVZY Daily Update

on:
  # 每天定时运行
  schedule:
    - cron: '0 0 * * *'  # 每天UTC 0点运行
  # 允许手动触发
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'info'

jobs:
  update-tv-channels:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 确保有写权限
    steps:
      # 第一步：检出代码（仅用于获取脚本，不使用git推送）
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: main
      
      # 第二步：设置Python环境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      # 第三步：安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests schedule
      
      # 第四步：生成文件并直接使用GitHub API上传（完全避免git操作）
      - name: Generate and upload file using GitHub API
        run: |
          echo "🔄 开始生成直播源文件..."
          
          # 运行脚本生成文件
          python tvzydaily.py
          
          # 验证文件生成
          if [ ! -f "tzydayauto.txt" ]; then
            echo "❌ 错误: tzydayauto.txt 文件未生成"
            exit 1
          fi
          
          echo "✅ 文件生成成功，内容统计："
          wc -l tzydayauto.txt
          head -n 5 tzydayauto.txt
          
          # 直接使用curl和GitHub API上传文件，不使用任何git命令
          echo "🔄 使用GitHub API直接上传文件..."
          
          # 编码文件内容为base64
          CONTENT=$(base64 -w 0 tzydayauto.txt)
          
          # 创建日期时间戳
          TIMESTAMP=$(date +'%Y-%m-%d %H:%M:%S')
          
          # 构建JSON数据，正确处理引号转义
          JSON_DATA=$(cat <<EOF
{
  "message": "Auto-update IPTV files - $TIMESTAMP",
  "content": "$CONTENT",
  "branch": "main"
}
EOF
)
          
          # 使用curl调用GitHub API
          RESPONSE=$(curl -X PUT \
              -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              https://api.github.com/repos/wangchongzhq/zhby/contents/tzydayauto.txt \
              --data-binary "$JSON_DATA")
          
          # 检查响应
          if [[ $? -eq 0 ]]; then
            # 检查是否包含错误信息
            if echo "$RESPONSE" | grep -q '"message":"Not Found"'; then
              echo "❌ 错误: 仓库路径或文件路径不正确"
              echo "响应内容: $RESPONSE"
              exit 1
            elif echo "$RESPONSE" | grep -q '"message":"Bad credentials"'; then
              echo "❌ 错误: GitHub Token 无效或权限不足"
              echo "响应内容: $RESPONSE"
              exit 1
            else
              echo "✅ ✅ ✅ 成功使用GitHub API更新文件！"
              echo "响应内容: $RESPONSE"
            fi
          else
            echo "❌ GitHub API请求失败"
            echo "响应内容: $RESPONSE"
            exit 1
          fi
          
          echo "🎉 更新完成！"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
