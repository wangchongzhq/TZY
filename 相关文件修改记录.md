# 直播源验证工具 - 相关文件修改记录

## 修改原则
- **全局考虑**：每次修改时必须考虑相关文件的同步更新
- **关联检查**：修改核心功能时同步检查相关依赖文件
- **协调更新**：确保UI、逻辑、配置、启动脚本等协调一致

## 核心文件关联关系

### 验证引擎模块
- `integrated_validator.py` ← → `iptv_validator.py`
  - 核心验证逻辑与高级检测方法的整合
  - 保存路径设置需要同步修改
  
### 用户界面模块  
- `integrated_validator.py` ← → `web_app.py`
  - 双保存模式实现参考web_app.py
  - UI组件和交互逻辑保持一致

### 启动和构建模块
- `启动验证工具.bat` ← → `integrated_validator.py`
  - 启动脚本必须与主程序文件名匹配
  - EXE构建时需同步更新启动逻辑
  
- `build_exe.py` ← → `一键构建EXE.bat`
  - PyInstaller配置与构建脚本协调
  - 依赖包和隐藏导入设置同步

### 配置文件
- 各种`.py`文件 ← → `.gitignore`
  - 新增输出文件需要更新忽略规则
  - 构建产物目录管理

## 修改检查清单

### 每次修改前需要确认：
- [ ] 修改的核心功能涉及哪些相关文件？
- [ ] 启动脚本是否需要更新？
- [ ] 配置文件是否需要调整？
- [ ] 构建脚本是否需要同步？
- [ ] UI组件是否需要协调更新？

### 每次修改后需要验证：
- [ ] 启动脚本能否正确找到主程序？
- [ ] 相关文件中的路径引用是否正确？
- [ ] 构建过程是否包含所有必要文件？
- [ ] 用户操作流程是否完整可用？

## 文件依赖矩阵

| 功能模块 | 主要文件 | 关联文件 | 同步要求 |
|---------|---------|---------|---------|
| 核心验证 | integrated_validator.py | iptv_validator.py | 验证逻辑+保存路径 |
| 用户界面 | integrated_validator.py | web_app.py | 保存模式+UI组件 |
| 程序启动 | 启动验证工具.bat | integrated_validator.py | 文件名+启动逻辑 |
| EXE构建 | build_exe.py | 一键构建EXE.bat | 依赖配置+构建流程 |

## 修改历史记录

### 2026-01-04 - 修复启动脚本编码和GUI环境问题
**问题**：启动验证工具时出现编码错误和GUI启动失败
- 批处理文件编码解析错误（UTF-8字符被错误解释）
- integrated_validator.py是GUI应用，在无GUI环境中启动失败
- 错误信息显示乱码和命令解析错误

**修复措施**：
- 修复启动验证工具.bat的编码问题（使用GBK编码）
- 改进启动脚本的错误处理和用户指导
- 添加GUI环境检查和失败原因分析
- 提供更详细的故障排除建议

**相关文件**：
- validator/启动验证工具.bat - 修复编码和错误处理
- 相关文件修改记录.md - 更新维护说明

**预防措施**：
- 建立批处理文件编码规范
- 为GUI应用添加环境检查
- 提供清晰的错误信息和解决建议

## 维护说明
- 本文档应随每次重大修改而更新
- 新增功能时需要更新依赖关系
- 发现问题时应回溯相关文件修改记录
- 特别注意批处理文件的编码格式